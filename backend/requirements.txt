# subgrad 2.0 Backend — Python Dependencies
# Pin-free format for maximum flexibility. Use pyproject.toml for pinned installs.

# Web framework & ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Core math engine — Zero-Hallucination Policy enforcement
sympy>=1.13.0
numpy>=2.0.0

# Data validation & settings management
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Environment variable loading
python-dotenv>=1.0.0

# Gemini AI — Socratic tutor engine (Phase 2)
google-genai>=1.0.0

# Dev / Testing (comment out for production)
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
